# ==============================================================
# Dependances pour le projet TFE LeRobot SO-101
# ==============================================================
# Installer avec : pip install -r requirements.txt
# LeRobot est installe separement (voir setup_env.sh)
# ==============================================================

# --- Vision et calibration camera ---
opencv-python>=4.8       # Capture, calibration, traitement d'images
numpy>=1.24              # Calcul matriciel

# --- Outils ---
wandb                    # Suivi d'experiences
rerun-sdk                # Visualisation 3D
imageio[ffmpeg]          # Lecture/ecriture video

# --- Extension V2 : HFDetector open-vocabulary (OWL-ViTv2) ---
# Necessaire pour le detecteur Hugging Face. ~1.5 Go au total (modele 600 Mo + torch).
# Installer avec : pip install transformers torch pillow
transformers>=4.40       # OWL-ViTv2 / autres detecteurs HF
torch>=2.2               # Backend pytorch (Apple Silicon MPS supporte)
pillow                   # Pretraitement image (input transformers)
